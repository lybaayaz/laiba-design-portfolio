import { writeFileSync } from 'node:fs'; const base=(process.env.VITE_SITE_URL||'https://laibaayyaz.com').replace(/\/$/,''); const routes=['/','/work/packaging','/work/branding','/work/marketing-editorial','/work/print-prepress']; const urls=routes.map(r=>`  <url><loc>${base}${r}</loc></url>`).join('\n'); writeFileSync('public/sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls}\n</urlset>\n`); writeFileSync('public/robots.txt',`User-agent: *\nAllow: /\nSitemap: ${base}/sitemap.xml\n`);